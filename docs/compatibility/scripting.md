# OXCE scripting compatibility

Phase 4 targets reference commit `4df3a5e571a1a4b5e8a46d3161fb2e21a2adba15`.
The generated [`script-inventory.json`](script-inventory.json) remains the broad source
discovery ledger. Exact template-resolved declarations are captured separately by the
`script-api-catalog` oracle and loaded by the managed runtime from that reviewed catalog.

The `script-core` oracle compiles the reference `src/Engine/Script.cpp`, not a grammar
reimplementation. `tools/capture-script-core-reference.ps1` records normalized compile
outcomes, reference log diagnostics, and final output-register values for deterministic
binding-free programs. This is specification evidence for upcoming work; no row claims
a managed implementation merely because an oracle exists.

## Core language

| Area | Status | Reference source | Fixture | Required behavior / next split |
|---|---|---|---|---|
| Whitespace, line tracking, and `#` comments | partial | `src/Engine/Script.cpp` (`ScriptRefTokens::getNextToken`) | `script-core` (`comment`); lexer unit fixtures | Managed tokenization ignores comments through newline and retains one-based line/column/offset spans. Compiler-level diagnostic parity remains. |
| Symbols, dotted references, and labels | partial | `src/Engine/Script.cpp` (`ScriptRefTokens`, `findOperationAndArg`) | syntax unit fixtures; `script-core` (`forward-label`, `duplicate-label`) | Symbol character and colon-label syntax is implemented. Receiver rewriting and semantic label creation/use remain compiler work. The reference oracle confirms forward and duplicate labels fail in the captured forms. |
| Statement terminator and token errors | partial | `src/Engine/Script.cpp` (`ScriptRefTokens::getNextToken`, `ScriptParserBase::parseBase`) | `script-core` (`invalid-number`, `missing-semicolon`, `invalid-punctuation`, `too-many-arguments`); syntax unit fixtures | Managed syntax requires semicolons, reports invalid tokens with spans, and enforces 16 arguments. Exact compiler diagnostic composition remains. |
| Decimal, hexadecimal, binary, and octal integers | compatible for lexical conversion | `src/Engine/Script.cpp` (`SelectedToken::parse`, numeric token parsing); `libs/rapidyaml/c4/charconv.hpp` | `script-core` (`locals-and-arithmetic`, `comment`, `invalid-number`, `integer-overflow`); integer unit fixtures | Prefixes and malformed forms are recognized, and conversion deliberately wraps modulo 32 bits like the pinned c4 conversion. Operation-level arithmetic remains VM work. |
| Text literals and escapes | compatible for compiled/runtime values | `src/Engine/Script.cpp` (`ScriptRefTokens::getNextToken`, `ScriptText`) | `script-core` (`invalid-text-escape`); lexer and runtime-frame fixtures | Double-quoted text accepts only escaped quote and backslash, rejects newlines and unterminated input, lives in immutable operand storage, and reaches context-aware providers through bounded frame scratch storage. |
| Primitive `int`, `text`, `label`, `null`, and separator types | compatible for runtime ABI | `src/Engine/Script.h` (`ArgEnum`, `ArgRegisteType`); `ScriptParserBase` constructor | `script-inventory`; `script-api-catalog`; type/overload and runtime-frame fixtures | Stable managed primitive IDs and register/writable/reference/editable modifiers represent all 94 emitted catalog forms. Scalar, text, and reference values cross the reusable frame ABI; separators participate in overload matching without runtime storage. Gameplay providers still own concrete custom-type behavior. |
| Registers, output registers, locals, and constants | compatible for compilation | `src/Engine/Script.h` (`ScriptWorkerBase`, `ParserWriter`); `src/Engine/Script.cpp` (`parseVar`, `parseConst`) | `script-core`; compiler/VM/API fixtures; script corpus | Scalar, custom, pointer, and editable-pointer declarations, typed constants, initialization including `null`, non-shadowing scopes, bounded aligned storage, nested-scope reuse, and scalar output copy compile. Reference/text runtime host frames remain with provider slices. |
| Required return and return arity | compatible for compilation | `src/Engine/Script.cpp` (`parseReturn`, `ScriptParserBase::parseBase`) | `script-core` (`return-only`, `missing-return`); default-script and multi-output fixtures | Non-empty programs require a terminal return. Bare `return;` preserves parser outputs; explicit returns require the complete arity and read all values before writing outputs. |
| Unreachable-code rejection | compatible for structured core flow | `src/Engine/Script.cpp` (`ScriptParserBase::parseBase`) | `script-core` (`unreachable`); compiler unit fixtures | Operations after return, break, or continue are rejected except for the matching structural boundary. Named-label flow remains outside the supported public core subset. |
| `begin`, `if`, `else`, and `end` blocks | compatible for scalar conditions | `src/Engine/Script.cpp` (`parseBegin`, `parseIf`, `parseElse`, `parseEnd`) | `script-core` (`conditional`, `combined-conditions`, `conditional-else-if`) | Nested scopes, six scalar comparisons, `and`/`or` clause aggregation, conditional `else` chains, terminators, and declaration placement are lowered to immutable IR. Host-bound condition operands remain catalog work. |
| `loop`, `break`, and `continue` | compatible for compilation | `src/Engine/Script.cpp` (`parseLoop`, `parseBreak`, `parseContinue`) | `script-core` (`counted-loop`, `loop-control`, `break-outside-loop`); API and execution-limit fixtures; private corpus | Counted scalar loops and catalog list loops lower with reference-compatible hidden initialization, bounds, typed outputs, nesting, break/continue targets, and bounded execution. Named-label loops are not exposed. Gameplay providers own list enumeration at runtime. |
| Arithmetic and assignment operations | compatible for scalar core and typed compilation | `src/Engine/Script.cpp` (`MACRO_PROC_DEFINITION`); `src/Engine/ScriptBind.h` | `script-core` (`set-output`, `locals-and-arithmetic`, `arithmetic-wrap`, `aggregate-offset`, `division-modulo`, `division-by-zero`); API fixtures; private corpus | Scalar assignment, swap, wrapping arithmetic, aggregate, offset/modulo, divide/modulo, and wide-intermediate multiply/divide execute with deliberate runtime failures for zero divisors. Custom-type operations sharing core names and the hidden typed value-copy binding compile as host calls; their execution remains provider-owned. |
| Comparisons and boolean aggregation | compatible for scalar core conditions | `src/Engine/Script.cpp` (`parseCondition`, `aggregate`) | `script-core` (`conditional`, `combined-conditions`) | All six comparisons and eager ordered `and`/`or` clause aggregation are compiled and executed. Binding expressions and separator-driven nested calls remain. |
| Bit, color, shade, wave, and math helpers | compatible for captured scalar cases | `src/Engine/Script.cpp` (`MACRO_PROC_DEFINITION`, helper functions) | `script-core` (`bit-operations`, `math-limits`, `discrete-waves`, `trigonometric-waves`, `color-and-shade`); VM unit fixtures | Shift, bit, power, root, absolute, limit, color, shade, and five wave families execute through exact integer-width helpers; invalid wave periods fail intentionally. Additional platform-sensitive conversion edges remain corpus work. |
| Overload resolution and argument separators | compatible for corpus compilation | `src/Engine/Script.cpp` (`findBestOverloadProc`); `src/Engine/Script.h` (`ArgCompatible`); `src/Engine/ScriptBind.h` | `script-api-catalog`; type/overload and API unit fixtures; private corpus | Writable/reference restrictions, typed receiver rewriting, unique-best scoring including editable/read-only pointer preferences, ambiguity, and literal `__` separators are implemented. List-loop separator expansion is implemented; general separator-driven nested call expressions remain outside the accepted corpus. |
| Compile diagnostics | partial | `src/Engine/Script.cpp` (`ScriptParserBase::parseBase`) | 13 rejected `script-core` cases; lexer, syntax, and compiler unit fixtures | Lexical, syntax, declaration, symbol, argument, control-flow, program-limit, and unreachable failures use structured codes with precise spans. Exact reference message composition and parser/parent identity remain. |
| Runtime errors and execution limits | compatible for current VM ABI | `src/Engine/Script.cpp` (`ScriptWorkerBase::executeBase`); `src/Engine/Script.h` limits | `script-core` (`division-by-zero`); compiler/VM/event/runtime-frame fixtures | Output, argument, register, compiled-instruction, execution-step, call-depth, trace-entry, and event-execution bounds are enforced. Caller outputs and writable arguments commit only on success. Missing providers, provider failures, and nested limit failures remain distinct and recursion reuses the exact bounded frame/catalog slice. |
| Deterministic execution trace and final values | partial | `src/Engine/Script.cpp` (`ScriptWorkerBase`, generated proc dispatch) | all accepted `script-core` cases; trace unit fixtures | The pinned oracle compares deterministic final output for 20 accepted programs. Managed execution optionally records stable semantic operation/source/result entries outside hot IR and bounds their count. Cross-engine semantic event traces remain. |

## Registration and integration

| Area | Status | Reference source | Fixture | Required behavior / next split |
|---|---|---|---|---|
| Core operation inventory | partial | `src/Engine/Script.cpp` (`ScriptParserBase` constructor, `MACRO_PROC_DEFINITION`) | `script-inventory`; expanded `script-core` | Generated ledger records 54 built-in names and six primitive/pseudo-types. Public binding-free scalar operations now have stable managed IDs and execution; internal dispatch operations and overload signatures must be reconciled with the API catalog. |
| `ScriptRegister` declaration expansion | compatible for catalog declarations | all reference `ScriptRegister` definitions reached by the public parser roots | `script-api-catalog`; [catalog review](../phase-4-api-catalog-review.md) | Executable metadata capture expands transitive registration helpers into 755 exact signatures. Gameplay provider behavior remains in owning vertical slices. |
| Binding and constant catalog | compatible for declarations | `src/Engine/Script.cpp` (`logScriptMetadata`); `ScriptBind.h`; owning `ScriptRegister` sites | `script-api-catalog`; API/host fixtures; [catalog review](../phase-4-api-catalog-review.md) | One embedded reviewed catalog declares stable IDs, complete emitted parameter forms, 132 constants, exact parser membership, custom types, and zero unresolved entries. Provider implementations remain deliberately separate. |
| Parser/event group catalog | compatible for emitted signatures | `src/Mod/ModScript.h`; parser construction in `src/Mod/Mod.cpp` | `script-api-catalog`; `script-content` | All 60 constructed parser instances record group, event support, typed outputs, typed inputs, constants, and callable membership. The seven registered default bodies compile into each content snapshot. |
| Global scripts and tags | compatible for content compilation | `src/Mod/Mod.cpp` (`ModScriptGlobal`); `src/Engine/Script.h` (`ScriptGlobal`) | `script-events`; `script-content`; `content-ownership`; generated case-only ZIP path; public/private corpus | File-ordered typed tags, build-session `tagsFile`, parser-visible constants, `RuleList`, owner limits, source visibility, initial globals, all five event mutations, offsets, and default/global compilation populate immutable runtime content without reparsing. Lightweight file scopes use stable parsed-document identity rather than path comparison, including when ZIP entry names differ only by case. Gameplay-owned value codecs remain Phase 5 work. |
| Rule-local script envelopes | compatible for cataloged owners | typed Phase 3 rule loaders; reference rule `ScriptRegister` methods | `script-content`; public/private corpus | Composed operations compile through exact owning parser groups, null removes inherited programs, legacy stat mappings generate reference-ordered source, initial tags validate transactionally, and `ScriptsCompiled` advances only after a clean linked graph. |
| Event scripts | compatible for scalar event composition and execution ABI | `src/Engine/Script.h` (`ScriptParserEventsBase`, worker event execution); `src/Engine/Script.cpp` (`loadEvents`, `releseEvents`) | `script-events`; event and runtime-frame fixtures | Stable offset order, negative/current/positive execution, `new`/`update`/`override`/`delete`/`ignore`, unknown-name severity, invalid offsets, and bounded execution match the pinned fixture. Span execution reuses one frame and commits the chain transactionally. Exact gameplay provider signatures remain vertical-slice work. |
| Mission and arc scheduling scripts | not started | `src/Mod/RuleMissionScript.cpp`, `RuleArcScript.cpp`, `RuleEventScript.cpp` | mission-event typed fixtures only | Typed scheduling declarations exist; runtime selection/state transitions and embedded code compilation need distinct fixtures. |
| Persistent script values | partial state seam | `src/Engine/Script.h` (`ScriptValuesBase`, `ScriptValues`); inventoried owners | tag/value unit fixtures; `script-inventory` | Gameplay can own bounded mutable integer slots against immutable typed tags; missing reads return zero, capture omits zero values, and restore validates the complete candidate state transactionally. A pinned value fixture plus Phase 5 YAML codecs, migrations, and unknown-field sidecars remain. |
| Gameplay binding implementations | partial dispatch seam | `src/Engine/ScriptBind.h`; inventoried `ScriptRegister` sites | API/host unit fixtures | Declared scalar bindings compile independently of providers. Installed providers update writable registers; missing providers and provider failures are distinct structured runtime outcomes. Exact declarations and gameplay implementations remain separate required work. |

## Harness workflow

From the repository root on Windows with Visual Studio C++ tools:

```powershell
.\tools\update-script-inventory.ps1
.\tools\capture-script-core-reference.ps1
.\tools\capture-script-events-reference.ps1
.\tools\capture-script-api-reference.ps1
```

The commands discover the conventional reference checkout, accept `-ReferenceRoot`,
and refuse a revision other than the pinned commit. Captures write under ignored
`artifacts/` directories by default. Updating committed expected output requires an
explicit `-OutputPath`; review semantic changes before changing a fixture.
