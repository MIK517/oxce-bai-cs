# YAML and ruleset compatibility

| Area | Status | Reference source | Fixture | Notes |
|---|---|---|---|---|
| Node kinds: map, sequence, scalar, null | partial | `src/Engine/Yaml.h`, `src/Engine/Yaml.cpp` | `yaml-reference-semantics`, `yaml-representative-normalization` | Immutable DOM and semantic normalization preserve order, duplicate entries, tags, and missing versus the four reference null spellings. Complex keys need broader fixtures. |
| Multiple YAML documents | compatible | `src/Engine/Yaml.cpp`, `src/Savegame/SavedGame.cpp` | `yaml-reference-semantics`, `yaml-representative-normalization` | Ruleset- and save-shaped multi-document streams parse and normalize like the reference. Full saves remain subsystem-level work. |
| Anchors, aliases, and merge keys | partial | `src/Engine/Yaml.cpp`, rapidyaml integration | `yaml-reference-semantics`; unit anchor-reuse coverage; optional private corpus | Backward aliases and mapping/sequence merges are bounded and resolved. Reused anchor names replace the preceding binding for later aliases, matching real OXCE mods. Cycles, merge ordering variants, and tag interactions remain. |
| Ruleset text encoding and empty files | compatible for observed corpus | `src/Mod/Mod.cpp`, `src/Engine/Yaml.h`, rapidyaml integration | unit legacy-encoding coverage; optional private corpus | Strict UTF-8 is preferred; legacy Windows-1252 is decoded on invalid UTF-8 input. Empty and comment-only `.rul` files compose as empty content. Other legacy encodings need evidence before support is added. |
| Scalar conversion and defaults | compatible | `src/Engine/Yaml.h`, `src/Engine/Yaml.cpp`, `src/Mod/LoadYaml.h`, rapidyaml `charconv.hpp`, rapidyaml `base64.cpp` | `yaml-reference-semantics`, `yaml-scalar-conversions`, `yaml-special-scalar-conversions` | Strings, signed/unsigned integer widths and wraparound, numeric prefixes, Boolean spellings/numeric fallback, decimal/special/hexadecimal floats, strict bounded Base64, missing defaults, and explicit null match the captured reference. |
| Generic enum conversion | compatible | `src/Engine/Yaml.h` | `yaml-container-conversions` | Enums use wrapped signed 32-bit numeric conversion and accept values not declared by the enum, matching the generic reference overload. Named string enums and their diagnostics remain owned by individual rule loaders. |
| Generic container conversion | compatible | `src/Engine/Yaml.h` | `yaml-container-conversions` | Sequence, mapping, pair, tuple, and fixed-array helpers preserve legacy replacement, child iteration, fixed arity, sorted-map, and first-duplicate behavior. |
| Semantic normalization | compatible | `src/Engine/Yaml.cpp`, rapidyaml tree traversal | `yaml-representative-normalization` | Stable JSON preserves node kinds, ordered/duplicate mapping entries, tags, resolved merges, document boundaries, and scalar spellings with explicit depth/output limits. |
| Deterministic emission | partial | `src/Engine/Yaml.h`, `src/Engine/Yaml.cpp`, `src/Savegame/SavedGame.cpp` | unit round-trip tests | Emits stable LF block YAML with bounded bytes/depth, multiple documents, scalar quoting, nulls, tags, and backward aliases. Complex keys, style parity, merge reconstruction, and full-save normalization remain. |
| Source locations and diagnostics | partial | `src/Engine/Yaml.cpp` | `malformed-location.yml`, unit tests | Parser and conversion failures carry source name and one-based line/column spans. Message parity remains. |
| Duplicate mapping keys | compatible | `src/Engine/Yaml.cpp` | `yaml-reference-semantics` | Entries are preserved in source order and ordinary lookup returns the first value. |
| Resource and recursion limits | partial | rapidyaml integration | unit tests | File bytes, nesting, nodes, documents, aliases, decoded Base64, normalized output, and emitted output have explicit configurable limits. Broader adversarial tests remain. |
| Mod discovery and metadata | compatible for file-backed discovery | `src/Engine/ModInfo.cpp`, `src/Engine/FileMap.cpp`, `src/Engine/Options.cpp`, `src/Engine/CrossPlatform.cpp` | `fixtures/public/mods/load-order`, generated ZIP/external-resource unit fixtures, optional private mod corpus | Directory and single-/multi-mod ZIP discovery implement metadata defaults, bounded scanning, lazy entry streams, archive-before-directory duplicate precedence, reference version normalization, single-master chains, missing-master/cycle pruning, persisted activation reconciliation, automatic master selection, required master/engine version diagnostics, and provenance. OXCE has no general dependency/conflict collections. Embedded archive injection remains a composition concern rather than filesystem discovery. |
| File lookup precedence and case behavior | compatible for file-backed layers | `src/Engine/FileMap.h`, `src/Engine/FileMap.cpp` | `vfs-layer-reference`, `fixtures/public/mods/load-order`, generated ZIP/external-resource unit fixtures | Active directory, ZIP, and external-resource layers preserve master-before-dependent and user activation order, with later layers winning and provenance retained. Loose external files override their archive, earlier `loadResources` entries override later entries, and the owning mod overrides both. Broader Unicode filesystem case fixtures remain. |
| Ruleset file load order | compatible for directory- and ZIP-backed mods | `src/Mod/Mod.cpp` (`Mod::loadMod`) | `fixtures/public/mods/load-order`, generated ZIP and case-only-path unit fixtures | Mod groups follow expanded activation order; each mod's `.rul` inputs are explicitly sorted by descending source path, matching the reference loader without dictionary enumeration. Parsed documents receive stable ordered identities, so script visibility remains file-local even when ZIP entry paths differ only by case. External resource rulesets are excluded. |
| Rule inheritance | partial | `src/Mod/Mod.cpp`, individual `Rule*.cpp` loaders | `fixtures/public/mods/rule-operations`, `typed-rule-replay`, family fixtures, malformed unit fixtures, private installation closure | Named-rule operation histories and bounded mapping-shaped `refNode` chains are preserved. Every registered named family owns recursive replay. Replayed identity/lifecycle keys are consumed without weakening ordinary nested-mapping audits; the installation-derived regression removes all 493 false unconsumed-property errors from the observed corpus. Broader family-specific merge variants remain. |
| List/map merge, replacement, and deletion | partial | `src/Mod/Mod.cpp`, `src/Mod/LoadYaml.h` | `fixtures/public/mods/rule-operations`, `typed-rule-replay`, malformed unit fixtures | Generic update-or-create, `new`, `override`, `update`, `delete`, `ignore`, index ordering, and soft failures are implemented. Typed readers match captured generic sequence/map replacement; family-specific incremental semantics remain with their loaders. |
| Cross-rule linking and missing references | compatible for the Phase 3 typed graph | `src/Mod/Mod.cpp` (`Mod::loadAll` after-load/link order), individual rule `afterLoad` methods, `src/Savegame/AlienMission.cpp` | family relationship fixtures; aggregate closure unit tests; public and private installation corpus | The aggregate catalog runs local and cross-family validators in dependency order and reports `linked` only for an error-free implemented graph. Missing rules resolved by the reference `afterLoad` pass remain blocking errors; unresolved identifiers retained for runtime selection are preserved and reported as `OXCE-MOD-0026` warnings. Mission waves match the UFO/deployment/no-object runtime behavior, and Ufopaedia targets use the complete type enum. Complete `xcom1` and 40k/Rosigma installations both reach `linked` with zero validation issues or errors. |
| Typed rule section registry and loading diagnostics | compatible for loader core | `src/Mod/Mod.cpp` (`Mod::loadResourceConfigFile`, `Mod::loadFile`, `Mod::loadRule`), `src/Mod/RuleItemCategory.cpp` | `typed-rule-replay`; registry, family, and malformed unit fixtures | All 38 generic named sections and eight special sections are registered with exact identity keys/behaviors. Every named family has a typed loader. Typed replay retains rule/update provenance, bounds property nodes, reports unconsumed properties, and explicitly diagnoses fields owned by later resource or scripting stages. |
| Presentation named rules | compatible for typed declarations | `src/Mod/RuleInterface.cpp`, `src/Mod/RuleMusic.cpp`, `src/Mod/RuleVideo.cpp`, `src/Mod/SoundDefinition.cpp`, `src/Mod/CustomPalettes.cpp` | `presentation-rules`; unit malformed/merge fixtures | Interfaces, music, cutscenes, sound definitions, and custom palettes preserve reference defaults, `refNode`, replacement, and append behavior. Sound offset references remain unresolved until shared sound-set ranges are known. |
| Localization and extra resource declarations | compatible for composition | `src/Mod/ExtraStrings.cpp`, `src/Mod/ExtraSprites.cpp`, `src/Mod/ExtraSounds.cpp`, `src/Language/Language.cpp` | `presentation-rules`; unit resource-validation fixtures | Extra strings flatten plural forms and overlay by language/key. Extra sprites retain append/delete, legacy single-image aliases, dimensions, files, and owner provenance; extra sounds append. Explicit declared paths have platform-neutral VFS validation. Decoding and offset publication remain. |
| Country, region, and globe-label rules | compatible for typed declarations and Phase 3 linking | `src/Mod/RuleCountry.cpp`, `src/Mod/RuleRegion.cpp`, `src/Savegame/WeightedOptions.cpp` | `campaign-start-rules`; unit merge/malformed/closure fixtures; private installation closure | Radian conversion, latitude normalization, area append/reset, mission-zone replacement, weighted updates/removal, base functions, events, and script envelopes retain reference semantics. Eager country-event links remain blocking; runtime region mission selections are retained with warning diagnostics when unresolved. |
| Unit legacy properties | compatible for observed TFTD corpus | `src/Mod/Unit.cpp` (`Unit::load`) | `personnel-tactical-rules`; staged TFTD content audit | The unit-level `zombieUnit` key shipped by TFTD is accepted and ignored because the pinned reference loader does not read it. Item-level zombie-unit declarations retain their separate typed semantics. |
| Base-facility rules | compatible for typed declarations and Phase 3 linking | `src/Mod/RuleBaseFacility.cpp`, `src/Mod/Mod.h` (`RuleListOrderedFactory`), `src/Mod/MapScript.h` | `campaign-start-rules`; unit relationship/closure fixtures; private installation closure | Constructor defaults, inheritance, editable names, incremental item costs, resource references, capacities, positions, replacement names, and deletion-aware default list order are retained. Eager facility and ammo links remain blocking; runtime research and build-cost item names are retained. Resource offsets and preserved vertical map-level data remain with their owning later work. |
| Starting-base and campaign globals | compatible for focused campaign-start subset | `src/Mod/Mod.cpp` (`loadFile`), `src/Engine/Yaml.cpp` (`emitDescendants`), `src/Savegame/GameTime.cpp` | `campaign-start-rules`; unit overlay fixtures | Six starting-base templates use shallow top-level overlay and difficulty fallback. Starting time/difficulty, funding, personnel/transfer costs, research gates, base requirements, defeat values, and name parts retain defaults and ordered updates. Remaining combat/AI/global tables belong to their owning slices. |
| Item rules | compatible for typed declarations and Phase 3 linking | `src/Mod/RuleItem.cpp`, `src/Mod/RuleDamageType.cpp`, `src/Mod/RuleStatBonus.cpp`, `src/Mod/Mod.cpp` (editable loaders and offsets) | `item-rules`; unit merge/malformed/relationship/closure fixtures; private installation closure | Constructor defaults, inheritance, deletion-aware ordering, strategic values, action/ammo/fuse configuration, damage alterations, editable collections/maps, resource ownership, battle-type side effects, and item-local validation retain reference semantics. Reference-eager research, unit, inventory, ammo, and item links remain blocking; category, country, and zombie-selection names retain runtime lookup behavior. Resource offsets and stat/item scripts remain separate. |
| Equipment and production rules | compatible for typed declarations and Phase 3 linking | `src/Mod/RuleItemCategory.cpp`, `src/Mod/RuleWeaponSet.cpp`, `src/Mod/RuleCraftWeapon.cpp`, `src/Mod/RuleCraft.cpp`, `src/Mod/RuleUfo.cpp`, `src/Mod/RuleResearch.cpp`, `src/Mod/RuleManufacture.cpp`, `src/Mod/RuleManufactureShortcut.cpp`, `src/Savegame/WeightedOptions.cpp`, `src/Mod/Mod.cpp` | `equipment-production-rules`; unit merge/default/malformed/relationship/closure fixtures; private installation closure | Item categories, weapon sets, craft weapons, crafts, UFOs, research, manufacture, and manufacture shortcuts preserve constructor defaults, inheritance, editable collections/maps, deletion-aware ordering, stat overlays, fixed slots, weighted events, shallow soldier overlays, local invariants, and reference eager/runtime link timing. Resource/script resolution and final manufacture-shortcut runtime use remain separate. |
| Personnel and tactical-unit rules | compatible for typed declarations and Phase 3 linking | `src/Mod/RuleInventory.cpp`, `src/Mod/Armor.cpp`, `src/Mod/RuleSkill.cpp`, `src/Mod/RuleSoldier.cpp`, `src/Mod/Unit.cpp`, `src/Mod/RuleSoldierBonus.cpp`, `src/Mod/RuleSoldierTransformation.cpp`, `src/Mod/RuleCommendations.cpp`, `src/Mod/Mod.cpp` | `personnel-tactical-rules`; unit merge/default/malformed/relationship/closure fixtures; private installation closure | Inventories, armors, skills, soldier types, units, bonuses, transformations, and commendations preserve constructor defaults, inheritance, family-specific ordering, signed/nonzero stat overlays, nullable immunities, movement/damage configuration, weighted weapons, shallow templates, editable requirements, events, award criteria, and reference eager/runtime link timing. Validators publish armor caches. Name pools, resources, expressions, and scripts remain separate; unsupported transformation `requiresBuyBaseFunc` input is explicitly deferred. |
| Terrain and deployment rules | compatible for typed declarations and Phase 3 linking | `src/Mod/RuleTerrain.cpp`, `src/Mod/MapBlock.cpp`, `src/Mod/MapScript.cpp`, `src/Mod/MCDPatch.cpp`, `src/Mod/AlienRace.cpp`, `src/Mod/RuleEnviroEffects.cpp`, `src/Mod/RuleStartingCondition.cpp`, `src/Mod/AlienDeployment.cpp`, `src/Mod/Mod.cpp` | `terrain-deployment-rules`; unit default/merge/malformed/relationship/closure fixtures; private installation closure | Terrains, map scripts, MCD patches, alien races, environmental effects, starting conditions, and deployments preserve constructor defaults, inheritance, editable collections, replacement/append semantics, command defaults, and eager/runtime relationship timing. Nested payloads remain bounded YAML; binary resources, runtime generation, random selection, and scripts remain separate. |
| Mission, event, and ufopaedia rules | compatible for typed declarations and Phase 3 linking | `src/Mod/UfoTrajectory.cpp`, `src/Mod/RuleAlienMission.cpp`, `src/Mod/RuleArcScript.cpp`, `src/Mod/RuleEventScript.cpp`, `src/Mod/RuleEvent.cpp`, `src/Mod/RuleMissionScript.cpp`, `src/Mod/ArticleDefinition.cpp`, `src/Mod/Mod.cpp` | `mission-event-rules`; unit default/merge/malformed/relationship/closure fixtures; private installation closure | UFO trajectories, alien missions, arc/event/mission/ad-hoc scripts, events, and Ufopaedia articles preserve the implemented typed semantics. Article targets follow the complete reference type enum, generic TFTD articles have no implicit target, mission waves retain optional spawn behavior, eager event-research links remain blocking, and runtime scheduling/selection IDs produce warnings when unresolved. Runtime scheduling, random selection, and script compilation remain separate. |
| Content loading capability stages | compatible through `runtime-linked` | `src/Mod/Mod.cpp` (`Mod::loadAll`, `loadModData`, `loadExtraResources`, `newSave`) and strategic rule `afterLoad` sites | typed-loader and aggregate unit tests; `resource-resolution`; `runtime-rule-linking`; compiled-cache round trips; public and private installation corpus | `composed`, `typed`, `linked`, `resources-resolved`, `scripts-compiled`, and `runtime-linked` are distinct and enforced. The staged 40k/Rosigma installation reaches `runtime-linked` with 24,623 resource descriptors, 12,542 projected rules/scripts, and zero errors; reference-defined runtime-only unresolved IDs retain their IDs and remain explicit warnings. A cache hit reconstructs this graph with fresh generation handles and reruns runtime linking; rejected cache input falls back to the same fresh-build path. |
| Harness canonical JSON | partial | fixture-tool implementation | `bootstrap-json` | Harness self-test only; it does not claim YAML compatibility. |
